# -----------------------------------------------------------------------------
# Required credentials
# -----------------------------------------------------------------------------
CLAUDE_CODE_OAUTH_TOKEN=
GOOGLE_API_KEY=
# Required for PRIVATE repository access. leave blank for public-only access.
GITHUB_TOKEN=
# Required for Cohere Reranker. leave blank to disable reranking.
COHERE_API_KEY=


# -----------------------------------------------------------------------------
# Configurable runtime settings
# -----------------------------------------------------------------------------
# EMBEDDING_MODEL: Gemini embedding model used for vector storage and search.
EMBEDDING_MODEL=gemini-embedding-001
# PIPELINE_MAX_CONCURRENCY: Limits concurrent ingest tasks.
PIPELINE_MAX_CONCURRENCY=10
# MCP_PORT: Host port exposed for the FastMCP server (used in docker-compose).
MCP_PORT=8080 
# FRONT_PORT: Host port exposed for the Frontend server (used in docker-compose).
FRONT_PORT=3000

# The following settings can usually be left as-is.
# Change only if you have specific needs.

# RQ_QUEUE_NAME: Redis queue name for ingest jobs. Change only if you manage
# multiple queue groups.
RQ_QUEUE_NAME=repo-ingest
# RQ_DEFAULT_TIMEOUT: Seconds before a worker job times out.
RQ_DEFAULT_TIMEOUT=1800
# RQ_RESULT_TTL: Seconds to keep finished job metadata in Redis.
RQ_RESULT_TTL=86400
# REPO_STATUS_TTL: Optional TTL for repository status records (blank disables).
REPO_STATUS_TTL=
# EMBEDDING_OUTPUT_DIM: Override embedding dimensionality if required by Qdrant.
EMBEDDING_OUTPUT_DIM=
# EMBEDDING_BATCH_SIZE: Batch size when generating embeddings.
EMBEDDING_BATCH_SIZE=100
# QDRANT_API_KEY: Supply when your Qdrant instance requires authentication.
QDRANT_API_KEY=
# QDRANT_COLLECTION_NAME: Name of the Qdrant collection storing snippet vectors.
QDRANT_COLLECTION_NAME=snippet_embeddings
# QDRANT_UPSERT_BATCH_SIZE: Batch size for vector upsert operations.
QDRANT_UPSERT_BATCH_SIZE=100
# QDRANT_FACET_LIMIT: Maximum repositories returned when listing completions.
QDRANT_FACET_LIMIT=1000
# Set False when your environment supports Docker in Docker (DinD).
# This enables background workers to run in separate containers.
USE_SUBPROCESS=true

# -----------------------------------------------------------------------------
# Fixed internal service wiring (NEVER CHANGE)
# -----------------------------------------------------------------------------
# REDIS_URL: Points to the Redis container declared in docker-compose.yaml.
#            NEVER CHANGE THIS VALUE.
REDIS_URL=redis://redis:6379/0
# QDRANT_URL: Points to the Qdrant container declared in docker-compose.yaml.
#             NEVER CHANGE THIS VALUE.
QDRANT_URL=http://qdrant:6333
